// const { Client } = require("pg");

// const client = new Client({
//   connectionString: process.env.DATABASE_URL, // Use environment variable for the database URL
//   ssl: {
//     rejectUnauthorized: false, // Needed for connecting to hosted PostgreSQL databases
//   },
// });

// client
//   .connect()
//   .then(() => console.log("Connected to the database"))
//   .catch((err) => console.error("Connection error", err.stack));

// module.exports = client;
